// Environment configuration
// In future we can use react-native-dotenv, but for now a simple object works.

export const ENV = {
    // Mobile emulator needs 10.0.2.2 for local, but we are prioritizing CLOUD now as requested.
    // User wants to test directly against Render.
    API_URL: 'https://decision-maker-f0zw.onrender.com',
};
